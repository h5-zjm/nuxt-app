import protectedRoute from "#server/protectedRoute";

export default defineEventHandler(async (event)=>{
    // await protectedRoute(event)
    // console.log('test',getRequestURL(event))
    // throw createError({
    //     statusCode:401,
    //     message:'恭喜你'
    // })
})
